{
  "name": "library-management-portal",
  "version": "1.0.0",
  "description": "Library Management Portal - Frontend Layout Fix Tests",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "fast-check": "^3.15.0",
    "http-server": "^14.1.1"
  }
}
